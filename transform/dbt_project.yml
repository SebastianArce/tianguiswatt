name: tianguiswatt
version: "1.0.0"
profile: tianguiswatt

model-paths: ["models"]

vars:
  # Ofgem Typical Domestic Consumption Value, medium household electricity (kWh/year),
  # July 2026 review.
  tdcv_kwh: 2500

models:
  tianguiswatt:
    staging:
      +materialized: view
    marts:
      +materialized: table
