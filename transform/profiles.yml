renewablepulse:
  target: dev
  outputs:
    dev:
      type: clickhouse
      driver: http
      host: "{{ env_var('CLICKHOUSE_HOST', 'localhost') }}"
      port: 8123
      user: "{{ env_var('CLICKHOUSE_USER', 'renewablepulse') }}"
      password: "{{ env_var('CLICKHOUSE_PASSWORD', 'renewablepulse') }}"
      schema: "{{ env_var('CLICKHOUSE_DB', 'renewablepulse') }}"
      secure: false
      threads: 4
